<p id="notice"><%= notice %></p>

<h1>Pedidos</h1>

<table>
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Data</th>
      <th>Valor total</th>
      <th>Descontos</th>
      <th>Acrescimos</th>
      <th>Valor frete</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pedidos.each do |pedido| %>
      <tr>
        <td><%= pedido.cliente %></td>
        <td><%= pedido.data %></td>
        <td><%= pedido.valor_total %></td>
        <td><%= pedido.descontos %></td>
        <td><%= pedido.acrescimos %></td>
        <td><%= pedido.valor_frete %></td>
        <td><%= link_to 'Show', pedido %></td>
        <td><%= link_to 'Edit', edit_pedido_path(pedido) %></td>
        <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pedido', new_pedido_path %>
